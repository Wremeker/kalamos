/**
 * Exercises are not part of the OSS core. This stub keeps the block-operations
 * code path compiling; interactive blocks should be provided via the block
 * plugin API and supply their own initial data through `BlockPlugin.initialData`.
 */
export function getInitialExerciseData(_typeId?: number): Record<string, unknown> {
  return {};
}
