/**
 * Media URLs returned by the UploadAdapter are expected to be absolute, so the
 * editor does not need a configured API origin. API_URL is exported as an empty
 * string for the few utilities that prefix relative paths.
 */
export const API_URL = '';
